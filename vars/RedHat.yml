# vim: ft=yaml ts=2:
---

apache:
  package: "{{ apache_scl_prefix + '-httpd' if apache_scl_prefix is defined else 'httpd' }}"
  service: "{{ apache_scl_prefix + '-httpd' if apache_scl_prefix is defined else 'httpd' }}"

